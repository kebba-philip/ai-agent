[project]
name = "intelligent-agent"
version = "0.1.0"
description = "Build an AI code agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
